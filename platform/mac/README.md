# Mac platform

Owner: Mac

This neutral directory was created during the initial scaffold. The Mac owner may add sanitized client configuration, verification scripts, and public manifests here. Populated paths, host identity, private URLs, and credentials remain outside Git.
